# Kessho Product Patch Bridge Policy

## Scope

Exact Pad, Lead, and Drum parameter arrays are temporary parity bridges. They are allowed only while the Product Core finishes replacing legacy web/native tonal interpretation with generated Product Core preset IDs, generated Product Core preset metadata, and bounded user override fields.

This policy covers every exact patch count and array field currently present in C++, TypeScript, Swift, generated schema, module adapter patches, web snapshot encoding, and native snapshot encoding.

## Classification Values

- `CANONICAL_CORE_FIELD`: final Product Core-owned state, safe to keep as architecture.
- `TEMP_COMPAT_WEB_REFERENCE`: generated or adapter state copied from the web reference to preserve parity while Product Core reconstruction catches up.
- `TEMP_COMPAT_NATIVE_REFERENCE`: native-only compatibility state used to preserve ABI/snapshot shape while native is thin.
- `DEPRECATED_BRIDGE_FIELD`: host-authored exact patch state that must be retired and must not become the musical owner.

## Sunset Table

`scripts/check-kessho-product-patch-bridges.mjs` emits the same sunset inventory one entry per field/range to `docs/reports/kessho-product-patch-bridges.json`.

| Field name | Source family | Classification | Owner | Why it exists | Reconstructability from preset ID + user overrides | Retirement condition | Replacement Product Core owner |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `KesshoProductGeneratedSourcePreset.exact_pad_param_count`<br>`KesshoProductGeneratedSourcePreset.exact_pad_params[0..52]` | Pad | `TEMP_COMPAT_WEB_REFERENCE` | `scripts/generate-kessho-product-bindings.mjs`, generated C++ schema | Preserves factory web Pad patch parity while generated preset IDs are canonical. | Factory patch is reconstructable today by generated preset ID lookup; user edits still need bounded Pad override fields or events. | Retire when C++ Product Core reconstructs all shipped Pad presets from generated preset IDs plus structured Pad metadata and Pad probes pass without exact Pad arrays. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `KesshoProductSourcePreset.exactPadParamCount`<br>`KesshoProductSourcePreset.exactPadParams[0..52]` | Pad | `TEMP_COMPAT_WEB_REFERENCE` | Generated TS/Swift schema | Preserves factory web Pad patch parity for host and native lookup tables. | Factory patch is reconstructable today by generated preset ID lookup; user edits still need bounded Pad override fields or events. | Retire when generated host/native source preset tables no longer carry exact Pad arrays. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `KesshoProductSourceSnapshot.exact_pad_param_count`<br>`KesshoProductSourceSnapshot.exact_pad_params[0..52]` | Pad | `DEPRECATED_BRIDGE_FIELD` | Product snapshot C ABI and snapshot loader | Carries legacy host-authored Pad oscillator/filter/envelope overrides across the Product snapshot ABI. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is the legacy override payload. | Retire when Pad overrides use generated preset IDs plus bounded Pad override fields or live Product Core events. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `ProductSourceSnapshot.exactPadParamCount`<br>`ProductSourceSnapshot.exactPadParams[0..52]` | Pad | `DEPRECATED_BRIDGE_FIELD` | Web snapshot adapter and legacy preset compatibility bridge | Carries web-authored Pad exact override patches into Product snapshots. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is the legacy override payload. | Retire when web snapshot builders stop computing Pad tonal patches and send bounded Product Core override state. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `NativeProductSourceSnapshot.exactPadParamCount`<br>`NativeProductSourceSnapshot.exactPadParams[0..52]` | Pad | `TEMP_COMPAT_NATIVE_REFERENCE` | Native snapshot adapter | Preserves native serialization shape while native remains thin and does not author exact Pad arrays. | Native emits zero/default arrays; Pad state reconstructs from generated preset ID until bounded override fields/events exist. | Retire when native Pad snapshot serialization no longer carries exact Pad patch ABI fields. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `SourceState.exact_pad_param_count`<br>`SourceState.exact_pad_params[0..52]` | Pad | `DEPRECATED_BRIDGE_FIELD` | Product Core source state and snapshot loader | Stores decoded legacy Pad snapshot overrides inside Product Core source state. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is decoded legacy override state. | Retire when `SourceState` stores Pad preset ID plus bounded Pad override state instead of exact arrays. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `KesshoSourcePresetPatch.exact_pad_param_count`<br>`KesshoSourcePresetPatch.exact_pad_params[0..52]` | Pad | `TEMP_COMPAT_WEB_REFERENCE` | Shared module adapter boundary | Lets generated Pad preset patches and deprecated snapshot overrides cross into the shared Pad module. | Receives reconstructed Pad state from generated preset IDs today; final replacement is structured Pad preset/override state. | Retire when the shared Pad module accepts structured Product Core preset and override state directly. | C++ Product Core Pad source preset resolver and bounded Pad override/event owner |
| `KesshoProductGeneratedSourcePreset.exact_lead_param_count`<br>`KesshoProductGeneratedSourcePreset.exact_lead_params[0..79]` | Lead | `TEMP_COMPAT_WEB_REFERENCE` | `scripts/generate-kessho-product-bindings.mjs`, generated C++ schema | Preserves factory web Lead FM/operator/filter/envelope parity while generated preset IDs are canonical. | Factory patch is reconstructable today by generated preset ID lookup; user edits still need bounded Lead override fields or events. | Retire when C++ Product Core reconstructs all shipped Lead presets from generated preset IDs plus structured Lead metadata and Lead probes pass without exact Lead arrays. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `KesshoProductSourcePreset.exactLeadParamCount`<br>`KesshoProductSourcePreset.exactLeadParams[0..79]` | Lead | `TEMP_COMPAT_WEB_REFERENCE` | Generated TS/Swift schema | Preserves factory web Lead parity for host and native lookup tables. | Factory patch is reconstructable today by generated preset ID lookup; user edits still need bounded Lead override fields or events. | Retire when generated host/native source preset tables no longer carry exact Lead arrays. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `KesshoProductSourceSnapshot.exact_lead_param_count`<br>`KesshoProductSourceSnapshot.exact_lead_params[0..79]` | Lead | `DEPRECATED_BRIDGE_FIELD` | Product snapshot C ABI and snapshot loader | Carries legacy host-authored Lead FM/operator/filter/envelope overrides across the Product snapshot ABI. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is the legacy override payload. | Retire when Lead overrides use generated preset IDs plus bounded Lead override fields or live Product Core events. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `ProductSourceSnapshot.exactLeadParamCount`<br>`ProductSourceSnapshot.exactLeadParams[0..79]` | Lead | `DEPRECATED_BRIDGE_FIELD` | Web snapshot adapter and legacy preset compatibility bridge | Carries web-authored Lead exact override patches into Product snapshots. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is the legacy override payload. | Retire when web snapshot builders stop computing Lead tonal patches and send bounded Product Core override state. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `NativeProductSourceSnapshot.exactLeadParamCount`<br>`NativeProductSourceSnapshot.exactLeadParams[0..79]` | Lead | `TEMP_COMPAT_NATIVE_REFERENCE` | Native snapshot adapter | Preserves native serialization shape while native remains thin and does not author exact Lead arrays. | Native emits zero/default arrays; Lead state reconstructs from generated preset ID until bounded override fields/events exist. | Retire when native Lead snapshot serialization no longer carries exact Lead patch ABI fields. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `SourceState.exact_lead_param_count`<br>`SourceState.exact_lead_params[0..79]` | Lead | `DEPRECATED_BRIDGE_FIELD` | Product Core source state and snapshot loader | Stores decoded legacy Lead snapshot overrides inside Product Core source state. | Not fully reconstructable yet from preset ID plus bounded user overrides; this is decoded legacy override state. | Retire when `SourceState` stores Lead preset ID plus bounded Lead override state instead of exact arrays. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `KesshoSourcePresetPatch.exact_lead_param_count`<br>`KesshoSourcePresetPatch.exact_lead_params[0..79]` | Lead | `TEMP_COMPAT_WEB_REFERENCE` | Shared module adapter boundary | Lets generated Lead preset patches and deprecated snapshot overrides cross into the shared Lead module. | Receives reconstructed Lead state from generated preset IDs today; final replacement is structured Lead preset/override state. | Retire when the shared Lead module accepts structured Product Core preset and override state directly. | C++ Product Core Lead source preset resolver and bounded Lead override/event owner |
| `KesshoProductGeneratedSourcePreset.exact_drum_param_count`<br>`KesshoProductGeneratedSourcePreset.exact_drum_params[0..125]` | Drum | `TEMP_COMPAT_WEB_REFERENCE` | `scripts/generate-kessho-product-bindings.mjs`, generated C++ schema | Preserves factory web Drum patch parity while generated Drum voice preset IDs, morphs, and host-edited exact Drum controls remain bridged to Product Core. | `DrumDefault` is reconstructable by generated preset ID lookup; voice-family patches are reconstructable from drum voice preset A/B IDs plus morphs and covered by the source reconstruction proof. | Retire when Drum source patches reconstruct from generated Drum source preset ID, drum voice preset IDs, voice morphs, and structured drum metadata. | C++ Product Core Drum source and drum voice preset resolver |
| `KesshoProductSourcePreset.exactDrumParamCount`<br>`KesshoProductSourcePreset.exactDrumParams[0..125]` | Drum | `TEMP_COMPAT_WEB_REFERENCE` | Generated TS/Swift schema | Preserves factory web Drum parity for host and native lookup tables. | `DrumDefault` is reconstructable by generated preset ID lookup; voice-family patches are reconstructable from drum voice preset A/B IDs plus morphs. | Retire when generated host/native source preset tables no longer carry exact Drum arrays. | C++ Product Core Drum source and drum voice preset resolver |
| `KesshoProductSourceSnapshot.exact_drum_param_count`<br>`KesshoProductSourceSnapshot.exact_drum_params[0..125]` | Drum | `DEPRECATED_BRIDGE_FIELD` | Product snapshot C ABI and snapshot loader | Carries legacy host-authored Drum module overrides across the Product snapshot ABI so individual Drum voice controls reach Product Core. | Partially replaced: canonical Drum voice preset IDs and morphs reconstruct voice-family patches, but arbitrary exact Drum overrides still need bounded override fields/events. | Retire when Drum overrides use generated Drum voice preset IDs, voice morphs, bounded drum override fields, and live Product Core events. | C++ Product Core Drum source and drum voice preset resolver |
| `ProductSourceSnapshot.exactDrumParamCount`<br>`ProductSourceSnapshot.exactDrumParams[0..125]` | Drum | `DEPRECATED_BRIDGE_FIELD` | Web snapshot adapter and legacy preset compatibility bridge | Carries web-authored Drum exact override patches into Product snapshots. | Web emits `exactDrumParamCount = KESSHO_PRODUCT_DRUM_PARAM_COUNT` and maps generated Drum param specs from edited sliders until bounded Drum override fields exist. | Retire when web and Product Core no longer expose exact Drum snapshot fields. | C++ Product Core Drum source and drum voice preset resolver |
| `NativeProductSourceSnapshot.exactDrumParamCount`<br>`NativeProductSourceSnapshot.exactDrumParams[0..125]` | Drum | `TEMP_COMPAT_NATIVE_REFERENCE` | Native snapshot adapter | Preserves native serialization shape while native remains thin and does not author exact Drum arrays. | Native emits zero/default arrays; Drum state reconstructs through generated source preset ID plus Drum voice preset IDs and morphs. | Retire when native Drum snapshot serialization no longer carries exact Drum patch ABI fields. | C++ Product Core Drum source and drum voice preset resolver |
| `SourceState.exact_drum_param_count`<br>`SourceState.exact_drum_params[0..125]` | Drum | `DEPRECATED_BRIDGE_FIELD` | Product Core source state and snapshot loader | Stores decoded legacy Drum snapshot overrides inside Product Core source state. | Partially replaced: Drum voice preset IDs and morphs reconstruct voice-family patches; arbitrary exact Drum overrides still need bounded state. | Retire when `SourceState` stores Drum preset IDs, voice morphs, and bounded Drum override state instead of exact arrays. | C++ Product Core Drum source and drum voice preset resolver |
| `KesshoSourcePresetPatch.exact_drum_param_count`<br>`KesshoSourcePresetPatch.exact_drum_params[0..125]` | Drum | `TEMP_COMPAT_WEB_REFERENCE` | Shared module adapter boundary | Lets generated Drum preset patches, Drum voice reconstruction, and deprecated snapshot overrides cross into the shared Drum module. | Receives reconstructed Drum state from generated source and voice preset IDs today; final replacement is structured Drum preset/override state. | Retire when the shared Drum module accepts structured Product Core Drum preset and override state directly. | C++ Product Core Drum source and drum voice preset resolver |

## Source Reconstruction Proof

- Drum voice preset bridge: `cpp/KesshoCore/src/product/sources/DrumSource.cpp` reconstructs a Drum voice patch from `preset_id`, `drum_voice_preset_a_ids`, `drum_voice_preset_b_ids`, and `drum_voice_morphs` when `exact_drum_param_count` is zero.
- `cpp/KesshoCore/tests/ProductSourceWrapperTests.cpp` proves the `Soft Touch` sub voice preset ID path renders the same audio as an equivalent exact `exact_drum_params[0..125]` snapshot patch.

## Canonical Replacement Fields

The canonical path for source identity and musical ownership is:

- `preset_id` / `presetId`: `CANONICAL_CORE_FIELD`
- `morph`, `distance`, `expression`, `level`, sends, post-LPF, stereo width, and hold seconds: `CANONICAL_CORE_FIELD`
- `drum_voice_preset_a_ids`, `drum_voice_preset_b_ids`, `drum_voice_morphs` / `drumVoicePresetAIds`, `drumVoicePresetBIds`, `drumVoiceMorphs`: `CANONICAL_CORE_FIELD`
- Generated Product Core source preset profile fields: `CANONICAL_CORE_FIELD`

The snapshot adapter may map legacy preset keys into these generated fields. It may not be treated as the final owner of oscillator, filter, envelope, FM, or Drum tonal behavior.

## Enforcement

- `scripts/check-kessho-product-patch-bridges.mjs` fails if an exact patch field is not listed here with a classification and retirement condition.
- Web snapshot exact patch builders must carry a `PATCH_BRIDGE_RETIREMENT` label.
- Native snapshot exact patch serialization must carry a `PATCH_BRIDGE_RETIREMENT` label and must remain zero/default unless a dedicated Product Core override policy is added.
- New exact patch fields are forbidden unless this document classifies them and the static check is updated in the same change.
