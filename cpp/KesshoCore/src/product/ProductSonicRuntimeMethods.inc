  void clearStepFieldOverride(LaneState& lane, uint32_t field, uint32_t step);
  void setStepFieldOverride(LaneState& lane, uint32_t field, uint32_t step, float value, float value2, float value3 = 0.0f, float value4 = 0.0f, uint32_t flags = 0u);
  void applySequencerStepEvent(const KesshoProductEvent& event);
  void applySynthArpConfigEvent(const KesshoProductEvent& event);
  void applySynthArpStepEvent(const KesshoProductEvent& event);
  void applyCommitSynthArpPatternEvent(const KesshoProductEvent& event);
  void applyScatterVoiceParamEvent(const KesshoProductEvent& event);
  void commitScatterConfig();
  void setScatterEnabled(bool enabled);
  ProductScatterPhrase generateScatterPhrase(uint32_t voice, uint32_t seed) const;
  void scheduleScatterEvents(uint32_t frames, SequencerBuffer& out, bool include_inactive_sources);
  void beginSceneProgram(const KesshoProductEvent& event);
  void setSceneProgramEntry(const KesshoProductEvent& event);
  void setSceneProgramCommandHeader(const KesshoProductEvent& event);
  void setSceneProgramCommandValues(const KesshoProductEvent& event);
  void commitSceneProgram();
  void setSceneProgramPosition(float position);
  void scheduleSceneRuntimeEvents();
  void beginJourneySchedule(const KesshoProductEvent& event);
  void setJourneyScheduleEntryHold(const KesshoProductEvent& event);
  void setJourneyScheduleEntryMorph(const KesshoProductEvent& event);
  void beginJourneyTransitionProgram(const KesshoProductEvent& event);
  void setJourneyTransitionEntry(const KesshoProductEvent& event);
  void setJourneyTransitionCommandHeader(const KesshoProductEvent& event);
  void setJourneyTransitionCommandValues(const KesshoProductEvent& event);
  void commitJourneyTransitionProgram(const KesshoProductEvent& event);
  void commitJourneySchedule();
  void setJourneyScheduleEnabled(bool enabled);
  void completeJourneySchedule();
  void scheduleJourneyRuntime();
  uint64_t nextJourneyScheduleFrame() const;
  void configureGlobalAutoCycle(const KesshoProductEvent& event);
  void scheduleGlobalAutoCycle();
  uint64_t nextGlobalAutoCycleFrame() const;
  void applyScheduledSequencerEvolution(
      LaneState& lane,
      uint32_t target_id,
      uint32_t lane_index,
      uint64_t cycle);
  void applyParityEvolveSequencerLaneEvent(const KesshoProductEvent& event);
  void applyDiceSequencerLaneEvent(const KesshoProductEvent& event);
  void applyJourneyStateEvent(const KesshoProductEvent& event);
  void resetSonicRuntimeState();
  void configureSourceMorphAutomation(uint32_t target_id, bool enabled, ProductMorphMode mode, float phrases_per_cycle, uint32_t seed);
  void disableSourceMorphAutomationForSource(uint32_t source_id);
  void disableSourceMorphAutomationForDrumVoice(uint32_t voice_index);
  void applySourceMorphAutomationValue(uint32_t target_id, float value);
  void scheduleSourceMorphAutomation();
  void configureProductAutoStop(bool enabled, double duration_seconds);
  uint64_t nextAutoStopFrame() const;
  void applyAutoStopAtCurrentFrame();
  void beginRoutingMuteGroups(const KesshoProductEvent& event);
  void setRoutingMuteGroupSlot(const KesshoProductEvent& event);
  void commitRoutingMuteGroups(const KesshoProductEvent& event);
  void setRoutingMuteGroupsEnabled(bool enabled);
  void recallRoutingMuteGroup(uint32_t slot_index, uint32_t transition_frames = UINT32_MAX);
  void recallRoutingMuteGroupAt(uint32_t slot_index, uint32_t transition_frames, uint64_t start_frame);
  void applyRoutingMuteSceneCommands(const ProductRoutingMuteSceneCommands& commands);
  void applyRoutingMuteDiscreteState(uint32_t slot_index);
  void scheduleRoutingMuteGroups(uint32_t frames);
  uint64_t nextRoutingMuteGroupFrame() const;
  float routingMuteGainForFrame(uint32_t row, uint64_t absolute_frame);
  bool routingMuteRowSuppressed(uint32_t row) const;
  uint32_t routingMuteRowForSource(uint32_t source_id) const;
