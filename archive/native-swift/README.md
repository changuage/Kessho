# Native Swift Archive

This directory contains the historical standalone SwiftUI port and its old
verification scripts/docs. It is not part of the active Product Core, Web TS,
or Capacitor lanes.

Do not wire active npm scripts, Product Core CI, or Capacitor packaging through
these files. If this work resumes, move it back into an explicit active lane in
its own change set.
